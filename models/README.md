# Model checkpoints

Trained `.pt` files are **not committed** (size). Train with `train_marl_los_angeles.py` or keep checkpoints locally.

To share a specific checkpoint, use [GitHub Releases](https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases) or [Git LFS](https://git-lfs.com/).
