# Model checkpoints

**Tracked in git (same as the final report):**

| File | Use |
|------|-----|
| `marl_cologne_shared_dqn_regionaware_v8_scale07.pt` | Experiment 1 (scale 0.7, ratio 0.5, Δt=5 s) |
| `marl_cologne_shared_dqn_regionaware_v16_demand085_di10.pt` | Experiment 2 + training-dynamics figures (scale 0.85, MARL Δt=10 s) |

All other `.pt` files stay local (see root `.gitignore`). Train or resume with `train_marl_los_angeles.py`.
